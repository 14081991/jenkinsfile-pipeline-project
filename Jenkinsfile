pipeline {
    agent any
    stages {
        stage('run') {
            steps {
                echo 'Clarusway_Way to Reinvent Yourself'
                sh 'python3 --version'
                sh 'echo "alemde tek bir ses- ölümüne ESES"'
                sh 'python3 pipeline.py'
            }
        }
    }
}